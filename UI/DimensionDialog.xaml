<Window x:Class="BusbarCAD.UI.DimensionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enter Inside Dimension" Height="200" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Enter Inside Dimension" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

        <TextBox Grid.Row="1" Name="txtDimension" FontSize="16" Padding="5"
                 TextChanged="TxtDimension_TextChanged"/>

        <TextBlock Grid.Row="2" Name="txtCurrent" Text="Current: 0mm" Margin="0,10,0,0" Foreground="Gray"/>

        <TextBlock Grid.Row="3" Name="txtValidation" Text="" Margin="0,5,0,0"
                   Foreground="Red" TextWrapping="Wrap" Visibility="Collapsed"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Name="btnOK" Content="OK" Width="75" Height="30" Margin="0,0,10,0"
                    Click="BtnOK_Click" IsDefault="True"/>
            <Button Name="btnCancel" Content="Cancel" Width="75" Height="30"
                    Click="BtnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
